services:
  - type: web
    name: openstate-ai
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn cleanstate.wsgi:application"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: False
      - key: PYTHON_VERSION
        value: 3.11.0